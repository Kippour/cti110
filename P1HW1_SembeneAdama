# Adama Junior Sembene
# June 25, 2025
# P1HW1
# This program calculates an exponent and performs an addition/subtraction sequence based on user input.

print("-----Calculating Exponenets----\n")

base = int(input("Enter an integer as the base value: "))
exponent = int(input("Enter an integer as the exponent: "))

power = base ** exponent
print(f"\n{base} raised to the power of {exponent} is {power} !!\n")

print("-----Addition and Subtraction----\n")

start = int(input("Enter a starting integer: "))
add_num = int(input("Enter an integer to add: "))
sub_num = int(input("Enter an integer to subtract: "))

final_result = start + add_num - sub_num
print(f"\n{start} + {add_num} - {sub_num} is equal to {final_result}")
